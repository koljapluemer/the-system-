Add a number of primaryTypes (c.f. assets/note_schema.json) and some functionality as described below.
All types should be integrated in the schema, and get their own lists entry and CRUD forms.

Let's start by simply integrating the following types, w/ edit forms that are just title+content: `gestalt`, `context` `ifThen`, `description`, `quote`, `source`, `entity`